System.Int32 Solti.Utils.DI.Proxy.Tests.ProxyGeneratorTests.IFoo<System.Int32>.Prop
{
    get
    {
        System.Object result = Invoke(FProp.GetMethod, new System.Object[0], FProp);
        if (result == CALL_TARGET)
            return Target.Prop;
        return (System.Int32)result;
    }

    set
    {
        System.Object result = Invoke(FProp.SetMethod, new System.Object[]{value}, FProp);
        if (result == CALL_TARGET)
            Target.Prop = value;
    }
}