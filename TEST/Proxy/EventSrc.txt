class Test
{
    private static readonly System.Reflection.EventInfo FEvent = GetEvent("Event");
    event Solti.Utils.DI.Proxy.Tests.TestDelegate<System.Int32> Solti.Utils.DI.Proxy.Tests.IFoo<System.Int32>.Event
    {
        add
        {
            System.Object result = Invoke(FEvent.AddMethod, new System.Object[]{value});
            if (result == CALL_TARGET)
                Target.Event += value;
        }

        remove
        {
            System.Object result = Invoke(FEvent.RemoveMethod, new System.Object[]{value});
            if (result == CALL_TARGET)
                Target.Event -= value;
        }
    }
}